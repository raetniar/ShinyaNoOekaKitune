# Agent ログ アーカイブサマリー (〜 2026/08/26)

## 📌 概要
本ドキュメントは、これまでに実施された作業ログ（全65件）から、**決定事項、実装パターン、修正履歴**を体系的にまとめた統合要約です。
ライブラリ化すべき恒久的な設計規約は [`README_important.md`](./README_important.md) に集約されています。

---

## 1. きりぬきつーる (`tools/きりぬきつーる/`) 開発・最適化サマリー

### ① Whisperローカル自己学習エンジンの実装 (2026/08/26)
- **概要**: ユーザーが字幕編集を行った際の修正差分（Whisper出力 vs ユーザー確定テキスト）をPC内（`system_files/learned/learned_data.json`）で完全ローカル学習するエンジンを開発。
- **機能**:
  - `vocabulary`: 配信者特有の固有名詞・口癖を自動抽出し、次回のWhisper文字起こし時に `initial_prompt` へ自動注入。
  - `corrections`: 誤字修正ペア（例: 「狐若」➔「初狐羽鹿」）を自動蓄積し、文字起こし完了直後に自動置換。
  - `stats` & 設定画面: 累計学習回数の表示およびワンクリック初期化ボタンを実装。

### ② Gemini 1.5 Flash プロンプト＆バズ分析仕様の高度化 (2026/08/26)
- **5大原則**: 1分完結、冒頭3秒フック、初見文脈自立性、感情ピーク重視、オチ＆ループ性。
- **5軸各20点個別採点 (`score_breakdown`)**:
  - `hook_stop_rate`, `standalone_context`, `emotional_peak`, `follow_through_power`, `loop_timing_quality` の5項目（合計100点）。
- **新項目**: サムネイル最適フレーム時刻（`thumbnail_frame`）、文脈自立性の自己監査根拠（`context_check`）。
- **UI連携**: 切り抜き候補一覧ボタンに `No.1 [98点] (01:15～) タイトル` のスコアバッジを表示。

### ③ メイン画面・ヘッダー・API連携UIの大幅改善 (2026/08/26)
- **Step 1 ヘッダー**: `対象動画:` 直下に `YouTube URL:` 入力欄と `🌐 開く` ボタンを新設し、プロンプト設定タブと双方向リアルタイム同期。
- **Step 1 コピペエリア下部**: `✨ Gemini APIで自動解析` と `📋 コピペから読み込む` を並列配置し、ワンクリック解析を実現。
- **全体設定タブ**: 開いてすぐの画面に `🔑 Google Gemini API Key 設定` 欄を大きく新設。変数 `gemini_api_key_var` による一元管理で入力即時保持・自動保存。
- **不要ボタン整理**: プロンプト設定タブ右下の重複していた解析ボタンを削除し、コピーボタンのみに整理。

### ④ スタンドアロン exe 化 ＆ ディレクトリ整理 (2026/08/26)
- **PyInstaller ビルド**: `CustomTkinter`, `Whisper`, `MoviePy`, `PyTorch` を完全同梱したスタンドアロン exe（約338MB）をビルド。
- **ディレクトリ整頓**: ルート直下は `きりぬきつーる.exe`, `動画/`, `ショート/`, `画像/`, `system_files/` のみに整理。ドキュメント類（PDF, 画像, 仕様書, ガイド）はすべて `system_files/docs/` に集約。

### ⑤ 商業ショート動画制作スタジオ向け拡張 & 音声認識強化 (2026/09/07)
- **ログ**: [`20260907_1036_ShortsProductionStudioUpgrade.md`](./20260907_1036_ShortsProductionStudioUpgrade.md)
- **概要**: ショート動画制作会社設立・受託運用に対応するため、ツールを商業プロダクション仕様（v3.0）へとフル拡張。
- **主要機能**:
  - **クライアント/案件プロファイル管理**: クライアントごとに固有の学習辞書・NGワード・設定を完全分離。
  - **映像レイアウト拡張**: 超高速ブラー背景、上下2画面（ゲーム実況標準）、中央クロップ、レターボックス。
  - **プロ演出 & 初稿ガード**: 進行プログレスバー（シークバー）描画、初稿SAMPLE透かしオーバーレイ。
  - **音響マスタリング**: EBU R128準拠 `-14 LUFS` ラウドネス自動正規化。
  - **プロ編集連携**: SMPTEタイムコード準拠 Premiere Pro シーケンスマーカー CSV 出力。
  - **納品パッケージ自動生成**: 専用フォルダに動画、SRT、CSV、厳選サムネイルPNG、納品仕様書 `info.txt` を自動同梱。
  - **コンプライアンス & NGワード検品**: 字幕エディタ上での赤色警告バナーおよび該当字幕ボックスの赤枠ハイライト表示。
  - **バズ実績アナリティクス逆学習**: 高再生・高維持率パターンのGeminiプロンプト動的注入。
  - **高精度音声認識**: DCオフセット除去 & ピーク0.95正規化、ハルシネーション完全排除、長文字幕の読点スマート時間按分分割。

### ⑥ ヘッダー開閉トグル ＆ ステップ自動折りたたみ (2026/09/09)
- **ログ**: [`20260909_1320_KirinukiHeaderCollapsible.md`](./20260909_1320_KirinukiHeaderCollapsible.md)
- **概要**: メイン上部の案件・動画設定ヘッダーを手動で開閉（▼ / ▶）可能にし、「② 字幕・編集」および「③ 書き出し・ログ」へのステップ遷移時にヘッダーを自動収納。作業領域の縦幅を大幅拡大。
- **機能**:
  - 案件ラベル左端に「▼」トグルボタンを設置し、クリックでいつでも折りたたみ。
  - 折りたたみ時はスリムなサマリーバー（「▶ 案件・動画設定を展開」、案件名/動画名要約、クイック「作業保存」ボタン）を表示。
  - 「② 字幕・編集」または「③ 書き出し・ログ」に切り替えた際は自動的に折りたたまれ、「① 切り抜き候補」に戻ると自動で再展開。

### ⑦ 候補切り替え時の字幕同期・誤上書き完全防止 (2026/09/09)
- **ログ**: [`20260909_1328_FixJobSwitchSubtitleSync.md`](./20260909_1328_FixJobSwitchSubtitleSync.md)
- **概要**: 切り抜き候補セレクター（No.1 ➔ No.2 など）切り替え時に、旧ウィジェット破棄時の `<FocusOut>` イベントによって前候補の字幕データが新候補へ誤上書きされ字幕が反映されなかった不具合を完全修正。
- **対策**:
  - 切り替え中・レンダリング中のガードフラグ（`is_switching_job`, `is_rendering_subtitles`）の導入。
  - 旧ウィジェット破棄前のイベント明示的解除（`unbind`）とリスト先行クリア。
  - `save_current_editor_to_active_job` の保存対象インデックス明示化。
### ⑧ 字幕クイック編集（パレット）の入力欄横幅・左端位置揃え (2026/09/09)
- **ログ**: [`20260909_1335_AlignSubtitleInputWidth.md`](./20260909_1335_AlignSubtitleInputWidth.md)
- **概要**: プレビュー連動の字幕クイック編集（透過パレット画面）において、タイムコード表示の文字幅可変によりテキスト入力欄の開始位置がデコボコになっていた問題を解消。
- **対策**:
  - タイムコード文字列を常に固定桁数（`00:00.00 - 00:01.50`、常に19文字・Consolas等幅）に統一。
  - 各行を Grid レイアウトへ移行し、タイムコード列（minsize=150px固定）・入力欄（横幅100%拡張・sticky="ew"）・削除ボタンを完全分離。全行で入力欄の横幅および左右端がピシッと一直線に整列。

### ⑨ パレットへの編集モード（画面レイアウト）および画像追加・画像演出の完全復元 (2026/09/09)
- **ログ**: [`20260909_1340_RestoreLayoutAndImageOverlayToPalette.md`](./20260909_1340_RestoreLayoutAndImageOverlayToPalette.md)
- **概要**: 編集パレット（ドロワー/ポップアップ）の「スタイル設定」タブに、画面レイアウト（編集モード）および画像演出・画像追加機能が欠落していた問題を完全解消。メイン画面の画像演出タブにも手元の画像を選んで追加できる「＋ 追加」ボタンを新設。
- **対策**:
  - パレット最上部に「【画面レイアウト (編集モード)】」（ブラー背景/黒帯/上下2画面/中央クロップ）プルダウンを新設（メイン画面と双方向同期）。
  - パレット下部に「【画像演出 (オーバーレイ)】」セクションを新設（プリセット選択プルダウン、「＋ 画像追加」ボタン、フォルダ展開、リロード、位置XY・倍率スライダー、リセットボタン）。
  - エクスプローラーから画像ファイルを選択して「画像」フォルダへ自動コピー＆即時適用する `add_custom_overlay_image` を実装。
  - メイン画面の画像演出タブにも青色の「＋ 追加」ボタンを新設。両画面のスライダー・選択状態が完全双方向同期。

### ⑩ Step 2ヘッダーナビゲーションのレスポンシブ最適化 (2026/09/09)
- **ログ**: [`20260909_1345_Step2HeaderNavigationResponsive.md`](./20260909_1345_Step2HeaderNavigationResponsive.md)
- **概要**: ウィンドウ横幅を縮小した際に、切り抜き候補の選択プルダウンが膨張して「次へ ▶」ボタンが画面外へ押し出される不具合を解消。
- **対策**:
  - `select_box` 内で「次へ ▶」ボタン（`next_job_btn`）を `side="right"` で最優先固定パック。
  - 切り抜き候補プルダウン（`job_select_menu`）を `width=115` かつ `dynamic_resizing=False` に設定し、「No.X」が確実に視認できるコンパクト幅に抑制。
  - 左側タイトルラベルを「【3. プレビュー＆字幕編集】」にスリム化。横幅縮小時も「次へ ▶」ボタンが常に右端に留まり操作性を死守。

### ⑪ ヘッダー領域のミニマル・統合型改修（案3） (2026/09/09)
- **ログ**: [`20260909_1405_HeaderMinimalPlan3Alignment.md`](./20260909_1405_HeaderMinimalPlan3Alignment.md)
- **概要**: 案件・動画・YouTube設定ヘッダーのボタン数を半減（9個 ➔ 4個＋1スイッチ）させ、各行の縦横ライン・高さ28pxを完全一致させてガチャガチャ感を完全解消。
- **対策**:
  - 案件の新規作成・削除をプルダウン末尾の選択肢に統合し、メイン画面の「新規」「削除」ボタンを撤去。
  - YouTube URL入力欄の `<FocusOut>` / `<Return>` による完全自動保存化を実装し、「💾 URL保存」ボタンを撤去。
  - 1行目右（作業保存/読込: 180px）、2行目右（参照/フォルダ: 180px）、3行目右（グレースケール: 180px枠内配置）と右側ボタングループの横幅を180pxに完全統一。全行の開始X・終了X・高さ28pxがピシッと揃い、極上の美しさと使いやすさを実現。

---

## 2. 裏ガチャシステム (`tools/裏ガチャシステム/`) 最適化サマリー

### ① 完全スタンドアロン化・WebSocket IRC安定化 (2026/08/25〜08/26)
- Node.jsサーバー不要の単一HTMLファイル（`index.html`）へ統合。
- Twitch IRC WebSocket 接続（Bot認証 / 匿名接続）の自動ハンドシェイク・切断時自動再接続を実装。

### ② UI/UX・アイコン・カラー全面刷新 (2026/08/26)
- 全画面からOS依存絵文字を撤去し、スタイリッシュなインラインSVGアイコンへ完全統一。
- トラック背景完全透明・滑らかな角丸サムのカスタムスクロールバーを全画面に適用。
- セレクトボックス・入力フォームのダークモード視認性を完全修正。

### ③ 配信運用・セーフティ機能 (2026/08/26)
- **配信中自動検知＆オフラインガード**: 配信オフライン時はチャット応答をミュートし、テスト時の誤爆を防止。
- **天井（Pity）リセット**: 2段階確認モーダルによる全リセット、および当選時個別リセット。
- **Discord連携**: 当選者リストの進捗管理（素材受取チェックリスト）とDiscordスレッド連携。
- **アクティブタイムアウト**: 15分無言リスナーの待機遷移。

---

## 3. TitleManager (`tools/Title_manager_localize_Ver0.4.html` / Ver3.0) サマリー

### ① 多言語対応＆UIコンパクト化 (2026/08/24)
- 言語セレクターを単一のプルダウン（JP/EN/ZH）に統合。
- コマンドタブを配信者の実運用フロー（配信管理 ➔ 広告 ➔ シャウトアウト ➔ モデレート ➔ コラボ）に最適化。
- タイトル日付自動挿入（`MM/DD`）の強制選択ロジックを実装。
- 制作者表記を「初狐羽鹿 / uikouka」に統一し、Twitch Manager 販促カードを配置。

---

## 4. 過去ログ集約・アーカイブ実績 (全65件)

以下の期間に作成された全作業ログは本ドキュメントおよび `README_important.md` に集約・ライブラリ化され、リポジトリ軽量化のため安全にクリーンアップされました：

- **2026/08/20 〜 2026/08/24（21件）**: TwitchResizer, TitleManager, 拡張機能LP, UI標準化
- **2026/08/24 〜 2026/08/26（44件）**: 裏ガチャシステム統合, きりぬきつーる自己学習・Gemini5軸採点・exe化・UI最適化・Gitプッシュ

---

## 5. StreamTalkAssistant (`tools/StreamTalkAssistant/` & `StreamTalkAssistant_shigu/`) サマリー

### ① ゆる狐AI「ようか」完全連携 ＆ 366日記念日辞書 (2026/08/31)
- 4表情PNGTuberアニメーション（待機・発話・まばたき・笑顔）、OBS透過オーバーレイ（`overlay.html`）、Twitch IRC自律対話、366日記念日辞書を実装。

### ② 対話学習・セリフ評価キーバインド（T1/T2）システム (2026/09/01)
- **キーバインド即時評価**: `T1` / `1` (Good / 模範回答)、`T2` / `2` (Bad / NG回答)、`T3` / `3` / `0` (未評価クリア) による直近発言の即時評価。
- **評価マネージャー画面 (`evaluation.html`)**: 対話ログ一覧表示、キーボードレビュー（上下移動・T1/T2）、理想のセリフ編集（`E`キー）、JSON保存機能。
- **AI思考エンジンへの還元**: Good/Bad評価されたセリフをFew-ShotプロンプトとしてGeminiに自動注入し、対話精度を継続向上。
- **UI連携**: 学習ページを別タブから同一画面内タブに統合（3大タブ: 対話学習・リスナーカルテ・設定）。

### ③ リスナーカルテ機能の大幅強化 (2026/09/01)
- **コメント履歴・重要事項の表示復元**: `recentComments` と `chatLogs` 逆引きで直近コメント履歴を各カードに表示。CRMデータベース（好物・ゲーム・役割）のバッジ表示を修復。
- **Listener_Notes.md 一括インポート機能**: 「ノート読込」ボタンから `Listener_Notes.md` を読み込み、全リスナー情報を自動パース＆カルテに一括反映。
- **好物マイニング語彙拡張**: 「うまい」「おいしい」「最高」「昨日〇〇食べた」など多様な語形に対応。
- **パーサー汎用化**: `**好物**: 〇〇`、`**ゲーム**: 〇〇`などの構造化パターンを自動認識。ハードコードを廃止。
- **debounce化**: 高頻度チャット時のDOM再描画を500msデバウンスで最適化。
- **フォルダ管理**: `tools/StreamTalkAssistant/` と `tools/StreamTalkAssistant_shigu/` を `.gitignore` に追加。実運用は `G:\マイドライブ\【02_UikoUka_活動】\パートナー計画\StreamTalkAssistant\` に移管。

---

## 6. 裏ガチャシステム追加修正サマリー (2026/08/28)

### ① ストリームリセットタイマー修正・Discord埋め込み形式修正 (2026/08/28)
- **ストリームリセットタイマー**: 配信終了検出後のガチャ自動リセットタイマーのバグ修正。
- **Discord埋め込み**: Webhook送信フォーマット（`embeds`配列）の不正形式を修正。
- **コマンドクールダウン**: クールダウン秒数をUI設定画面から変更可能に。

---

## 7. 整理整頓ログ (2026/09/01)

### フォルダ・Git管理状況
| パス | 状態 |
|---|---|
| `tools/StreamTalkAssistant/` | ✅ `.gitignore` 除外（実運用: パートナーフォルダ） |
| `tools/StreamTalkAssistant_shigu/` | ✅ `.gitignore` 除外 |
| `tools/TwitterPostGenerator/` | ✅ `.gitignore` 除外（未整理） |
| `tools/goal_customizer_preview.html` | ✅ `.gitignore` 除外（未整理） |
| `Agent/Archive/` | ✅ 2026/08/28以前のログを移動済み |
| 現在のアクティブログ | `20260831_1127_*`, `20260831_1455_*`, `20260901_1215_*` |

---

### ⑧ ヘッダーUIのミニマル統合 ＆ レイアウト整列 (2026/09/09)
- **ログ**: [`20260909_1410_RemoveFolderButtonAndExpandVideoSelect.md`](./20260909_1410_RemoveFolderButtonAndExpandVideoSelect.md)
- **案件管理の統合**: 新規・削除ボタンを案件プルダウン末尾（`[＋ 新規案件...]`, `[✕ 案件削除...]`）に統合し、ヘッダーのボタン数を大幅削減。
- **YouTube URL自動保存**: `<FocusOut>` / `<Return>` による完全自動保存化を実装し、「URL保存」ボタンを撤去。
- **重複ボタン撤去 ＆ 右ブロック完全整列**:
  - 「動画 参照...」ボタンの右隣にあった「📂 フォルダ」ボタンを撤去。
  - 「動画 参照...」ボタンの横幅を `width=180`（上段の「作業保存」「作業読込」ボタングループの合計幅と完全一致）に拡大し、高精細ベクターライクな `video` アイコンを付与。
  - 1行目右（作業保存88px + 作業読込88px = 180px）、2行目右（動画参照180px）、3行目右（グレースケールスイッチ180px枠）の3段すべてで横幅180pxの縦ラインが一直線に揃う美しい統一レイアウトを実現。
### ⑨ 編集ツールボタンの被り解消 ＆ プレビュー領域右上配置 (2026/09/09)
- **ログ**: [`20260909_1420_FixDrawerToggleButtonPositionAndOverlap.md`](./20260909_1420_FixDrawerToggleButtonPositionAndOverlap.md)
- **被り問題の解消**: コンパクトモード時に相対座標（`rely=0.18`）で配置されていた「☰ 編集ツール」ボタンが、上部時間制御バーの「⏱ 微調整 ▼」ボタンに覆いかぶさっていた不具合を完全解決。
- **プレビュー右上への確実な配置**: ボタンの親を `self.preview_container` に変更し、上端10px・右端から2%（`relx=0.98, y=10, anchor="ne"`）のプレビュー領域右上余白に固定配置。上段ボタン群との物理的な重複を100%防止。
### ⑩ パレット内画像演出への角度・不透明度スライダー追加 (2026/09/09)
- **ログ**: [`20260909_1425_AddOverlayAngleSliderToPalette.md`](./20260909_1425_AddOverlayAngleSliderToPalette.md)
- **角度・不透明度スライダーの追加**: ポップアップ／パレット（ドロワー）内の「画像演出 (オーバーレイ)」設定欄に「角度 (-180° 〜 +180°)」および「不透明度 (0.00 〜 1.00)」スライダーを追加。
- **双方向完全同期**: メイン側のタブ2「画像演出」およびパレット側の双方でスライダー操作・数値ラベル・プレビュー描画がリアルタイム同期。
- **リセット機能の拡張**: 「画像の位置・回転・倍率をリセット」により、角度（0°）や不透明度（1.00）も含めてワンクリックで初期化可能に。
### ⑪ 動画書き出し形式選択（焼き込み済みMP4 / 素材分離レイヤーMOV）実装 (2026/09/09)
- **ログ**: [`20260909_1430_ExportModeSeparateLayersTransparentMOV.md`](./20260909_1430_ExportModeSeparateLayersTransparentMOV.md)
- **出力形式セレクター新設**: Step 3 の設定エリアに「出力形式:」プルダウンを追加。
  1. `🎬 焼き込み済み (MP4 1本) 【基本】` （デフォルト）: 映像・音声・字幕・画像をすべて焼き込んだ完成版ショート動画。
  2. `📦 素材分離 (元動画MP4 ＋ 透過レイヤーMOV)`: 元動画MP4（背景映像＋音声正規化済み）と、字幕・画像を合成したアルファチャンネル付き透過QuickTime MOV（`-c:v png -pix_fmt rgba`）の2本を出力。
  3. `🗂️ 完全分離 (元動画MP4 ＋ 透過字幕MOV ＋ 透過画像MOV)`: 背景映像、透過字幕MOV、透過画像MOVの3本に完全分離。
- **外部NLE編集対応**: Premiere ProやAfter Effectsにドラッグ＆ドロップするだけで、動画トラックと字幕・画像トラックが透過レイヤーとしてタイムライン上で即座に微調整可能。
- **最新スタンドアロンEXEの再ビルド・配備完了**。

### ⑫ 単語タイムスタンプ(words)連動による字幕・音声の高精度音ズレ解消エンジン実装 (2026/09/09)
- **ログ**: [`20260909_1440_AudioSubtitleSyncWordTimestamps.md`](./20260909_1440_AudioSubtitleSyncWordTimestamps.md)
- **音ズレ根本原因の完全解明と解消**:
  - ハードコードされていた早すぎるマイナスオフセット（-0.35s / -0.2s）を撤廃し、音声波形と100%一致する `0.00s` 基準に適正化。
  - Whisper の `word_timestamps=True` で取得した単語単位のミリ秒タイムスタンプ（`words`）をフル活用する高精度同期エンジンを実装。
  - 長文分割時（`smart_split_segment`）の「文字数による機械的均等割り」を廃止し、分割フレーズの先頭単語〜末尾単語のタイムスタンプに直接吸着。文中の無音ポーズ（沈黙）を完璧にスキップし、どんな緩急のある話し方でも声が出る瞬間にピタッと字幕が表示されるように改良。
- **微調整UIのミリ秒対応**:
  - メイン画面の微調整パネルに `[⏪ -0.5s] [-0.2s] [◀ -0.05s] [▶ +0.05s] [+0.2s] [⏩ +0.5s]` を配備（50ms単位の微調整が可能に）。
  - パレット（ドロワー/ポップアップ）内にも `⏱ 微調整: [-0.2s] [◀ -0.05s] [▶ +0.05s] [+0.2s]` バーを新設し、プレビューを聴きながらワンタップでジャストタイミングへ追い込めるUIを実現。
- **最新スタンドアロンEXEの再ビルド・配備完了**。

### ⑬ 書き出し設定の整理・不要機能オミットとパッケージ格納標準化 (2026/09/09)
- **ログ**: [`20260909_1450_ReorganizeExportSettingsAndOmitFeatures.md`](./20260909_1450_ReorganizeExportSettingsAndOmitFeatures.md)
- **不要機能のオミット（機能削除）**:
  - `進行プログレスバー`: 動画投稿サイト側の機能と重複するため完全削除。
  - `初稿SAMPLE透かし`: 個人利用では不要な受託向け機能のため完全削除。
  - `Premiere Pro マーカーCSV`: Ae用CSVと用途重複のため削除。
  - 出力形式プルダウンの「完全分離」を削除し、`🎬 焼き込み済み (MP4 1本) 【基本】` と `📦 素材分離 (元動画MP4 ＋ 透過レイヤーMOV)` の2択にスッキリ整理。
- **パッケージ自動格納の標準化**:
  - `package_delivery` を常時ON（デフォルト化）し、UIチェックボックスは非表示に。
  - 書き出し時に動画1本ごとにフォルダ（`01_タイトル/`）が作られ、完成動画・素材分離MOV・字幕SRT・Ae用CSV・サムネPNG・仕様書TXTが確実にひとまとめに自動格納されるように統合。
- **チェックボックスの2列グリッド化（見切れ完全解消）**:
  - 残る4項目（字幕SRT、Ae用CSV、音圧正規化、字幕なし生動画）を `2行 × 2列` のゆったりしたグリッド配置に刷新し、ウィンドウ幅に関わらず文字見切れを100%根絶。
- **最新スタンドアロンEXEの再ビルド・配備完了**。

### ⑭ 完成動画＋字幕なし元動画(MP4 2本)出力モード新設 ＆ デフォルトチェックボックス最適化 (2026/09/09)
- **ログ**: [`20260909_1500_AddBothVideosExportAndDefaultCheckboxes.md`](./20260909_1500_AddBothVideosExportAndDefaultCheckboxes.md)
- **デフォルトチェックボックスの最適化**:
  - `[✔] 字幕SRT出力 (.srt)`: デフォルト **ON**
  - `[✔] Ae用CSV出力 (.csv)`: デフォルト **ON**
  - `[✔] 音圧正規化 (-14 LUFS)`: デフォルト **ON**
  - `[ ] 字幕なし (生動画のみ)`: デフォルト **OFF**（誤って字幕が消える事故を防止）
- **文字見切れの根絶**:
  - テキストラベルを簡潔に整え、右端の文字が一切見切れない美しい2列表示を実現。
- **「完成動画 ＋ 字幕なし元動画 (MP4 2本)」出力モードの新設**:
  - 出力形式プルダウンに `🎥 完成動画 ＋ 字幕なし元動画 (MP4 2本)` を追加。
  - 選択すると、字幕・画像入りの完成動画（`01_タイトル.mp4`）と、字幕なし元動画（`01_タイトル_raw.mp4`）の両方が同時に生成され、動画ごとのフォルダ内に確実にパッケージ格納される。
- **最新スタンドアロンEXEの再ビルド・配備完了**。

### ⑮ 幅狭時表示設定抜け落ち解消（作業タブ切替新設）＆ Ae用CSVデフォルト☑化 (2026/09/09)
- **ログ**: [`20260909_1730_FixNarrowDisplaySettingsAndDefaultAeCsv.md`](./20260909_1730_FixNarrowDisplaySettingsAndDefaultAeCsv.md)
- **幅狭・2カラム時（< 1180px）の表示設定抜け落ち根本解消**:
  - ウィンドウ幅を狭めた際にスタイル・演出設定列（`char_panel`）がウィンドウ外へ押し出されて消えていた問題を解消。
  - 大画面プレビュー（左列）を維持したまま、右側上部に `[ 📝 字幕リスト ]` ⟷ `[ 🎨 表示設定（レイアウト・文字・演出） ]` の切り替えタブ（SegmentedButton）を新設。
  - 「表示設定」を選ぶと、右列に「画面レイアウト（ブラー背景、黒帯、上下2画面、中央クロップ）」「フォント・文字色・縁取り」「画像演出・オーバーレイ」がワイドに展開。左列のプレビューをリアルタイムで見ながら設定を自由自在に変更可能に。
  - 幅1180px以上のワイド画面では従来の3カラム並列（プレビュー / 字幕 / 表示設定）に自動展開。
- **Ae用CSV出力 (.csv) のデフォルト☑（ON）化**:
  - `load_settings` のデフォルト値を `True` に是正し、起動時・設定読み込み時にも確実に初期チェックが入るよう改善。
- **最新スタンドアロンEXEの再ビルド・配備完了**。
