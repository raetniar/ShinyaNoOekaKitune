# 20260907_1243_LoudZoomAndAnchorUIAlign.md

## 目的
ユーザーからの指示に基づき、以下のUI配置・機能改修を実施し、最新の実行バイナリ（`tools/きりぬきつーる/きりぬきつーる.exe`）へビルド・反映する。
1. **大声ズームの右上横並び配置**:
   - 「太字 (Bold)」「斜体 (Italic)」の右横に「大声ズーム」チェックボックスを並べ、旧独立行を完全オミットして縦幅をスマートに圧縮。
2. **プレビューの上部ジャストフィット（下沈み解消）**:
   - プレビュー領域で `preview_phone_frame` を上寄せ（`rely=0.0, y=8, anchor="n"`）に固定し、画面下端に埋もれてしまう問題を解消。
3. **画像オーバーレイUIの簡素化・オミット対応**:
   - 手動パス入力と「参照...」ボタンをオミットし、「画像/」フォルダ固定運用（プリセット＋フォルダを開く＋更新）に集約。
   - `save_current_editor_to_active_job`、`load_job_to_editor`、`on_preset_selected` がオミット後も完全安全に連携するようリファクタリング。
4. **9点基準アンカーセレクター（□と十字のマス）の連携・描画対応**:
   - `app.py` のプレビュー描画および `video.py` のFFmpeg合成処理に9点アンカー（重心、上、下、左、右、左上、右上、左下、右下）の座標計算を完全実装。

---

## 変更ファイル一覧
- `system_files/src/app.py`:
  - `style_row` で太字・斜体・大声ズームの3項目を1行にすっきり横並び配置
  - `on_preset_selected` / `save_current_editor_to_active_job` / `load_job_to_editor` でのプリセット・アンカー安全処理
  - プレビュー描画での9点アンカー座標計算
  - `on_preview_container_configure` でのプレビュー上寄せ（`rely=0.0, y=8, anchor="n"`）
- `system_files/src/video.py`:
  - `process_single_clip` のオーバーレイ合成（字幕なし/あり双方）における9点アンカー座標計算の追加
- `tools/きりぬきつーる/きりぬきつーる.exe`:
  - PyInstaller による最新版ワンファイルexeの再ビルド＆デプロイ完了

---

## 検証結果
- `py_compile` による構文チェック通過（エラー0件）
- GUIコンポーネント配置テストスクリプト実行により、ウィジェット配置・アンカーセレクター・プリセット・プレビューフレーム上寄せを検証済み
- 最新exe（338,970,213 bytes）の配備完了
