# 作業記録: 画像演出のリセットボタン追加 & デフォルト中央配置

## 1. 目的
- 画像オーバーレイの位置（X/Y）、回転角度、拡縮倍率、不透明度、アンカーポイントを一括で初期状態へリセットできるボタンを追加する。
- 画像配置のデフォルト位置を画面の「縦横ど真ん中」（1080x1920基準で X=540, Y=960、アンカー「重心 (中央)」）に変更する。

## 2. 実施した変更
1. **リセットボタンの設置 & リセット機能の実装** (`src/app.py`):
   - スライダー群の直下に「位置・回転・倍率をリセット」ボタン（`overlay_reset_btn`）を追加。
   - `reset_overlay_transform(self)` メソッドを実装:
     - 位置 X: 540 px
     - 位置 Y: 960 px
     - 倍率: 100% (1.0)
     - 角度: 0° (0.0)
     - 不透明度: 1.00 (1.0)
     - アンカー: 「重心 (中央)」
     - 上記をスライダー・ラベル・active_job に即座に反映し、プレビュー画面（`show_current_frame`）を更新。
2. **デフォルト表示位置の縦横ど真ん中化**:
   - `overlay_x`: デフォルト `540`（キャンバス幅 1080 の 1/2）
   - `overlay_y`: デフォルト `960`（キャンバス高 1920 の 1/2）
   - スライダーの可動範囲を、画面内外へスムーズに動かせるよう最適化（X: -500〜1600px, Y: -500〜2500px, 倍率: 0.05〜3.0）。
   - `load_job_to_ui`, `sync_jobs_to_queue`, `create_queue_item_from_job`, レンダリング引数におけるフォールバック値を 100/0 から 540/960 に統一。

## 3. 検証結果
- `py_compile`: エラーなし（Exit 0）
- 自動テスト（モック検証）:
  - リセットボタン実行によるスライダー値、ラベル表記、アンカー、job辞書、プレビュー再描画呼び出しが全て期待通りであることを確認（ALL ASSERTIONS PASSED）。
- PyInstaller によるEXE再ビルドおよび配備完了。
