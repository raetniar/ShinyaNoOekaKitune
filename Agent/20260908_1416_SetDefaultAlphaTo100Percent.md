# 20260908_1416_SetDefaultAlphaTo100Percent.md

## 1. 目的
- ユーザー様からのご要望：
  > **「ここデフォルトでどっち100にしておいて・・・」**
- 初期状態（デフォルト）の透過度設定をドロワー・ポップアップ共に **100%（1.0・完全不透明）** に設定。
- 初回起動時やリセット時にも不透明（100%）でクッキリ表示され、ユーザーが動画を透かしたい場合のみヘッダーのスライダーで自由に透過度を下げられる仕様へ調整。

---

## 2. 変更内容
1. **`tools/きりぬきつーる/system_files/src/app.py`**:
   - `App.__init__` 内の `self.drawer_alpha` のデフォルト初期値を `0.75` から `1.0`（100%）へ変更。
2. **`tools/きりぬきつーる/system_files/config.json`**:
   - `"drawer_alpha": 1.0`、`"palette_alpha": 1.0` を設定。

---

## 3. 検証ログ
1. **動作テスト**:
   - `config_data` が空の状態（初期起動）での起動テストを実施。
   - `drawer_alpha == 1.0`（100%）で正しく初期化・表示されることを確認 ➔ **PASS**
2. **EXE 再ビルド＆配備**:
   - `uv run --python 3.9 ... pyinstaller --clean -y きりぬきつーる.spec` を実行（Exit Code 0）。
   - 最新バイナリ（338,983,013 バイト）をルートの `tools/きりぬきつーる/きりぬきつーる.exe` へ配備完了。
   - ビルド日時: **2026-09-08 14:15:54**
