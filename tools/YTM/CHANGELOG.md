# TwitchManager 更新履歴

このファイルには、利用者向けの主な変更を新しい順に記録します。詳しい使い方は[TwitchManager Wiki](https://github.com/MagnestGames/TwitchManager/wiki)を参照してください。

## 1.0.3-uka (作業ブランチ修正メモ) - 2026-08-05

### 機能改善・UI調整

- **タイトルカード分類名の自動反映**: プリセットの分類名を直接編集していない場合、入力した配信タイトルがそのままカードの分類名（`● [タイトル名]`）としてリアルタイムで自動取得・表示されるように修正。手動で分類名を指定した場合はそちらを優先表示。
- **SYNC 取得ボタンの配色統一**: 「SYNC 取得」ボタンを通常時・ホバー時ともに青系デザイン（ブルー）で統一。
- **識別タグ設定モーダル (`titleTagModal`) のレイヤー重なり (z-index) 表示不具合の修正**:
  - モーダル構造のクラスを `modal-overlay` へ統一し、最前面レイヤー (`z-index: 2000`) および背景外クリック閉じ処理を適用。
  - メインタブ下部の固定フッターボタン群がモーダル手前に被る表示崩れを解消。
- **配信タイトル「識別タグ・単語セット」自動置換機能＆ヘッダーインライン表示の実装**:
  - `{識別}`, `{識別A}`, `{カテゴリ}`, `{コラボ}`, `{日付}`, `{曜日}`, `{時間}` 等のタグをタイトルテンプレートに記述可能に。
  - テンプレートカードヘッダー内の指定位置（タイトル横）へ、含まれる識別タグをうっすらソフトにバッジ表示する機能を追加。
  - `{コラボ}` 自動置換時、IDリストの選択メンバーを名前昇順（A-Z）でソートし、` @AAA @BBBB` （半角スペース＋@ID）形式で自動連結。
  - 識別タグ設定モーダル (`titleTagModal`) およびタイトルのリアルタイム反映プレビュー機能を実装。
- **画像リサイズツールの確認ポップアップへの「※翔狐の別ツールサイトです」注記表示＆テキスト整理**:
  - 確認ダイアログメッセージ内へ「`※翔狐の別ツールサイトです`」を明記。
  - 重複していたURL文字列を整理し、視認性の高いハイパーリンクレイアウトに改修。
- **OBSドック・ポップアップブロック環境における外部リンク開放保証**:
  - 確認ダイアログ内に直クリック可能な直リンク (`https://raetniar.github.io/ShinyaNoOekaKitune/tools/Twitch_Image_Resizer_02.html ↗`) を直接埋め込み。
  - `safeOpenExternalUrl(url)` （要素生成・フォールバック開閉）を実装し、OBSドックのCEFセキュリティ制限や各種ブラウザのポップアップブロック下でも100%確実に外部ツールが開くよう完全対応。
- **Twitch画像リサイズツールへの外部リンク＆事前確認ダイアログの追加**:
  - CPタブのアクションバーおよび報酬編集モーダル内に「`🖼️ 画像リサイズ`」ボタン・リンクを追加。
  - 「`外部ツール (Twitch画像リサイズツール) を開きますが、よろしいですか？`」確認ダイアログを表示し、承認後に安全に別タブでツール URL (`https://raetniar.github.io/ShinyaNoOekaKitune/tools/Twitch_Image_Resizer_02.html`) を起動する処理を実装。
- **報酬ID (Reward ID) 表示および1タップコピー機能の追加**:
  - メイン表の各報酬サブ情報欄へ「`ID: xxxxxxxx... 📋`」バッジを表示。ワンタップで全IDをクリップボードへコピー可能に（StreamDeckや外部ツールの連携に便利）。
  - 報酬編集モーダル画面内にも「`報酬ID (ID)`」の閲覧・コピー欄を設置。
- **環境移行・PC/ブラウザ変更時のチャネポ「本ツール」作成タグ自動保護・バックアップ完全同期**:
  - `fetchTwitchCustomRewards()` にてClient-ID判定によるアプリ作成報酬の全自動検出＆ローカルストレージ自動保存処理を追加。
  - バックアップファイルのインポート復元時にも `cpAppRewardIds` をメモリおよびストレージへ確実に復元・統合。PCやブラウザを移行しても「`本ツール`」タグが失われず、編集・削除権限が確実に引き継がれる仕組みを構築。
- **ポップアップモーダル画面外クリックでの自動閉じ機能の完全統一**:
  - チャネポ追加・編集・一括編集・グループ編集モーダルを含むすべてのダイアログで、画面外の黒背景（バックドロップ）をタップ・クリックした際にモーダルが閉じる操作を追加。
- **一括編集画面の操作コントロールUI刷新 (プルダウンからON/OFFトグルボタンへ変更)**:
  - 一括編集内の「引き換え一時停止 / 再開」および「視聴者コメント必須」のUIをドロップダウン形式から直感的なセグメントトグルボタン (`cp-segmented-control`) へ完全刷新。
- **一括編集モーダル（cpBulkEditModal）への機能完全統合**:
  - カラー一括変更欄へ実体プレビュー、HEX入力、`🧪 スポイト`ボタン、使用中カラーパレットを完全組み込み。
  - 「`💬 視聴者コメント必須 (テキスト入力必須) を変更する`」一括設定カードを追加し、複数報酬のテキスト入力「必須 / 任意」を一括適用可能に。
- **視聴者テキスト入力必須化の設定対応 (is_user_input_required)**: モーダル内の「報酬名 (タイトル)」と「消費ポイント数 (コスト)」の間の直感的な位置へ「`💬 視聴者テキスト入力を必須化する`」チェックボックスを配置。
- **画面上の色を直接抽出する「スポイト（EyeDropper API）」ボタン機能**: カラー選択欄に「`🧪 スポイト`」ボタンを設置。画面上の配信素材や立ち絵からワンタップで色を即座に自動抽出可能に。
- **Twitch側での報酬変更とのリアルタイム即時同期 (EventSub連携)**: `channel.channel_points_custom_reward.add / update / remove` のリアルタイム通知を購読。Twitch公式画面側で報酬が操作された際も本ツールの表へ即時自動反映。
- **登録済み報酬のカラーパレット参照機能（使用中カラーの即時再利用）**:
  - モーダル（報酬作成・編集モーダルおよび一括編集モーダル）へ「`登録済み報酬のカラー (使用中):`」動的パレットを追加。
  - チャンネル内の全報酬で使用中のテーマカラーを自動抽出しパレット化。ワンタップで他の報酬と同じ色を参照・統一可能に。
- **カラーピッカー行の全要素ピクセルパーフェクト中央整列（.cp-color-picker-row）**:
  - カラーピッカー、実体プレビューボックス、HEX（16進数）入力欄、ラベルの全要素に `.cp-color-picker-row` スタイルを適用。
  - OSやWebKit環境のデフォルト装飾をリセットし、高さを28pxに固定・完全な垂直中央揃えへ修正。
- **「状態・切り替え」の一体型（一枠内3セグメント）コントロール化**:
  - 従来の「状態」「ON/OFF」「一時停止」を一つの列「`状態 / 切り替え`」内（一枠）へ完全統合。
  - `[ ON ]` (緑) / `[ ⏸ 停止 ]` (黄) / `[ OFF ]` (赤) の3セグメントで直感的に一発切り替え可能に。
  - テーブル最右列「`操作`」へ `✏️`（編集）および `🗑️`（削除）ボタンを正確に配置。
- **引き換え一時停止の表（テーブル）トグル列追加（黄色めボタン設計）**:
  - 報酬テーブル一覧の「ON/OFF」列の隣に**「一時停止」専用トグルスイッチ列**を追加。
  - トグル有効（一時停止中）時に黄色/アンバー色（`#f59e0b` / `#d97706`）でくっきり点灯するデザインを適用。グループカード内の「一時停止（⏸️）」ボタンも黄色テーマへ統一。
- **背景テーマカラー枠・コード・プレビューの垂直中央揃え＆高さ統一**: モーダル内のカラーピッカー、プレビューボックス、HEXコード入力欄のpx高さを28pxに完全統一し、美しい垂直中央揃えへ修正。
- **背景テーマカラーのリアルタイムプレビュー＆パレット強化**: 個別報酬の作成・編集モーダル内の「背景テーマカラー」に、実体カラープレビューボックス、HEX（16進数）直接入力、およびカラーパレットボタンを設置。
- **個別報酬編集での引き換え一時停止設定**: 個別CP報酬の作成・編集モーダル内にも「`⏸️ 引き換えを一時停止する (一時中断)`」チェックボックスを追加し、編集画面から直接一時停止状態で保存・作成可能に。
- **CP（チャンネルポイント）の引き換え一時停止・再開機能**:
  - ON/OFF（有効/無効）の切替に加え、Twitch API（`is_paused`）を用いた「一時引き換え中止（⏸️）」および「受付再開（▶️）」の操作に対応。
  - テーブル一覧での個別切替、グループカードでの「一括一時停止（⏸️）/ 一括再開（▶️）」、および一括編集モーダルでのまとめて変更に対応。
- **CP制限事項への注記追加**: ご案内・制限事項エリアへ「※ カスタムパワーアップは本ツールでは作成できません。」の注意事項テキストを追加（日本語・英語・簡体字対応）。
- **Twitch登録アイコン（画像）のUI表示対応**: Twitch Helix APIから取得したカスタム報酬画像およびデフォルトアイコン（`image` / `default_image`）を、CPテーブル一覧・グループカードの報酬タグ・グループ編集の選択チェックリスト内にアイコンとして鮮明表示。
- **CP（チャンネルポイント）の一括色変更・一括ポイント（報酬額）変更機能**: 
  - **個別選択編集**: テーブル一覧にチェックボックスを設置し、複数選択した報酬の「背景色（カラーパレット選択）」および「消費ポイント数」を一括変更できるアクションバーを追加。
  - **グループ一括編集**: 各CPグループカードに「一括編集（✏️）」ボタンを配置し、グループ内の全報酬を一括で色・ポイント変更可能に。
- **ヘルプ画面への最新版Wiki（GitHub）ボタン追加**: ヘルプ（？画面）のnoteリンク直下に、日本語・英語・簡体字それぞれの言語に対応した最新版Wikiへの直通ボタン（`🌐 最新版のWiki（GitHub）を開く`）を追加。
- **配信終了時のCPグループ自動OFF機能**: CPグループの自動化設定へ「配信終了時に自動OFF」を追加。EventSub（`stream.offline`）または配信状態チェックによる配信終了検知時、対象グループの報酬を一括で自動OFFへ切り替え。
- **CP（チャンネルポイント）ページの可読性向上**: ライトモード時の「有効(ON)」テキスト・「本ツール」バッジ・「全件ON」ボタン等の緑色要素のコントラスト比をWCAG AA/AAA基準を満たすエメラルドグリーン（`#047857`）へ強化。
- **トグルボタンの視覚フィードバック統合**: ライトモードにおけるON選択中ボタン背景を淡いラベンダー（`#e9d5ff`）へ調整し、ホバー・ON状態のスタイルルールを統合。

### 不具合修正

- **送信レイド時のチャット送信タイミング修正**: 配信終了後の `channel.raid` EventSub イベントで自動チャット送信される不具合を修正し、レイド準備中（Helix API `POST /raids` 実行時）のみにレイド先チャットメッセージが送信されるよう適正化。


## 1.0.2 - 2026-08-01

### 新機能

- TwitchManagerの音声を1本のOBSブラウザソースへまとめて出力できるようにしました。通知音設定の「🔊 OBSへ通知音をまとめる」から専用URLをコピーできます。
- 「お出迎え通知」を追加しました。指定したリスナーが配信のチャット参加者一覧に現れたとき、コメントをしていなくても、配信ごとに一度だけ指定の通知音を鳴らせます。
- 安定版の起動時に更新を確認する機能を追加しました。更新がある場合は「確認」「3日後に通知」「スキップ」から選べます。beta版では通知しません。
- TwitchManagerロゴをREADMEとインストーラーへ追加し、配信画面やバナーなどで利用できる素材として配布しました。
- サポーターリストへ、チャンネルポイントのカスタム報酬・自動報酬、Twitch Power-upで使われたBitsの記録と、配信ごとの過去ログを追加しました。

### 機能改善

- チャンネルポイント管理の案内を折り畳み式にし、報酬名を優先して読める一覧へ調整しました。操作ボタン、グループ名、作成元ラベル、スイッチの配置も狭い画面に合わせて整理しました。
- チャンネルポイント報酬へ、グループ一括ON/OFF、全件ON/OFF、並べ替え、既存報酬からの設定読み込み、配信開始・受信レイド時の自動ON、時間指定の自動OFFを追加しました。Twitch APIで変更できない外部作成報酬は、誤操作しないよう明確に区別します。
- お出迎え通知のTwitch ID入力で、レイド紹介などの履歴から候補を表示できるようにしました。
- レイド受付設定は、設定URLをコピーして既定のブラウザで開ける案内へ変更しました。
- 日付表示プリセットと、表示設定からレイド受付設定を含む機能ブロックを表示・非表示にする設定を追加しました。
- ダークモードとライトモードの配色、ボタン、スライダー、狭幅表示を見直しました。
- 日本語・英語・簡体字の表示と用語を見直しました。

### 不具合修正

- 送信レイド時の紹介処理とレイド先URLの案内を修正しました。
- OBSブラウザソース出力を有効にしたとき、TwitchManager本体との二重再生を避けるよう通知音の経路を統一しました。
- 途中まで実装されていたobs-websocket設定を削除し、復元データに古い設定が含まれていても引き継がないようにしました。
- チャンネルポイント一覧を含む複数の画面で、文字の縦表示、はみ出し、操作ボタンの不揃いを修正しました。
- EventSubの再接続・重複通知、配信開始判定、更新通知と復元画面のダイアログ競合を修正しました。
- バックアップから別アカウントの認証情報を持ち込まないようにし、空の認証情報を保存したときは接続を安全に停止するよう修正しました。

### インストーラー・ドキュメント

- Windows 11用インストーラーとmacOS 11以降用パッケージへ、OBS音声ブラウザソースとURL案内を同梱しました。
- 配布物はインストーラー本体へ集約し、個別のSHA256ファイル配布を終了しました。ハッシュ値はリリース本文で確認できます。
- READMEとWikiへ、OBS音声ブラウザソース、お出迎え通知、更新通知、ロゴ利用方法の説明を追加しました。
- リリース前の構文、ローカライズ、テーマ、主要機能、インストーラー構成の検証を拡充しました。

### アップデート前の確認

「その他」タブでバックアップを作成してから、最新のインストーラーを実行してください。同じ場所へ上書きインストールした場合、OBSへ登録済みのドックURLは通常そのまま利用できます。
