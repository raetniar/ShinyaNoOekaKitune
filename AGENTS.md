# AGENTS.md

## 適用範囲
このファイルはリポジトリ全体に適用する。Google Antigravity、Claude Code、Cursor、その他のAIは、調査、コード変更、ブランチ操作、コミット、Push、Issue/PR操作の前に本ファイルを確認し、指示に従うこと。

---

## 1. 基本原則・禁止事項（最重要）

- **Git操作の制限**
  - **GitへのPushは、ユーザーから明示的な指示があった場合のみ実行する。** 勝手なPushは厳禁。
  - コミット・ブランチ作成・履歴改変（`--force`, rebase, amend等）は、ユーザーの指示または明示的な承認なしに実行しない。
- **承認が必要な操作**
  - ファイルの削除、大幅なリファクタリング、外部ライブラリの追加・変更。
- **既存コードの保護**
  - 実装意図が不明な既存処理を勝手に削除・改変しない（推測で動かさず確認する）。
  - 他者または別AIの変更を上書き・破棄しない。

---

## 2. 作業計画と記録の管理（./Agent/）

- **記録先フォルダ**
  - 作業の予定と記録は、リポジトリルートの `./Agent/` フォルダ配下にマークダウン（`.md`）ファイルとして作成・蓄積する。
- **ファイル命名規則**
  - `YYYYMMDD_HHMM_[タスク名].md` （例: `20260820_1030_TwitchResizerHeaderFix.md`）
- **記載内容**
  - **目的**: 作業のゴールと対象ツール
  - **作業予定 / 実装方針**: 変更予定のファイル、採用するアプローチ
  - **変更履歴 / ログ**: 実際に変更した内容、実施した検証、確認結果
  - **残課題 / 備考**: 未確定事項や今後の検討点
- **ドキュメント体系と優先順位（重要度規約）**
  - **重要度**: **[`README_important.md`](file:///g:/マイドライブ/【04_素材・ツールライブラリ】/GIT_HTML/Agent/README_important.md) ＞＞＞ [`README.md`](file:///g:/マイドライブ/【04_素材・ツールライブラリ】/GIT_HTML/Agent/README.md)**
  - **`README_important.md` (ライブラリ情報・重要設計規約集)**:
    - ライブラリ化された情報の蓄積を最重視。ブランド定義、共通UI/UXシステム、API・IRC仕様、URL定義、法的要件等の恒久的なマスター資料。
  - **`README.md` (作業ログサマリー)**:
    - 日常のタスク履歴・作業ログ要約を蓄積。
- **アーカイブ運用＆定期削除ルール**
  - **ログの退避**: `./Agent/` 配下のログファイルが約10件以上蓄積した場合は、要点をサマリー（`README.md`）へまとめ、過去ログを `./Agent/Archive/` フォルダへ移動する。
  - **古いアーカイブの定期削除**: `./Agent/Archive/` 内の古いデータ（1ヶ月以上経過等）は、ライブラリ化すべき重要情報がないか確認し、ある場合は `README_important.md` へ抽出・追記した上で、適宜削除してリポジトリを軽量・クリーンに保つ。
  - **サマリーの定期整頓**: `README.md` が肥大化した場合は適切に整頓・分割する。

---

## 3. プロジェクト固有の技術規約（GIT_HTML）

- **ファイル同期ルール**
  - `tools/[ツール名]/[ツール名].html` と `index.html`、および外部CSS/JS（`css/style.css`等）など、関連する同一ツールの複数ファイルが存在する場合は、**必ず漏れなく同期して更新**すること。
- **プライバシー・ローカル処理の厳守**
  - 画像データ・入力データはすべてブラウザ内（ローカル環境 / localStorage等）で完結して処理し、無断で外部サーバーへ送信・保存するコードを作成しない。
- **デザイン・UI規約**
  - **デザインリファレンス最優先参照 (`Reference/`)**:
    - ボタンデザインやUIレイアウトの設計・実装にあたっては、リポジトリ内の **`Reference/` フォルダ（`Reference/CSS/` の各種CSSボタンデザイン集・PDF資料等）を最優先で参照**し、洗練された高品質なデザイン・ホバーエフェクト・マイクロインタラクションを採用すること。
    - なお、`Reference/` フォルダはローカル資料用のため **GitへのPush・コミット対象外（`.gitignore`）** とする。
  - **テーマ対応**: ライトモード／ダークモードの両方で文字・要素の視認性が保たれていること。
  - **レスポンシブ対応**: PC・タブレット（1024px以下）・スマートフォン（600px以下）の各画面幅でレイアウト崩れや文字被りが発生しないこと。
  - **ヘッダー仕様**: 原則として `position: sticky; top: 0;` を採用し、ヘッダー下のコンテンツ（免責事項バナー等）が隠れない構造を維持すること。
  - **アイコン・ビジュアル規約**: ユーザーからの明示的な指定がない限り**絵文字（OS依存フォント）はできる限り使わず、スタイリッシュなインラインSVGアイコン**（または洗練されたモダンUI要素）に変換・統一して使用すること。

---

## 4. 検証と報告

- **検証の実施**:
  - 変更内容に応じた動作確認・表示確認・レスポンシブ確認を行う。
- **報告内容**:
  - 変更したファイル一覧、変更のポイント、確認結果を簡潔にユーザーへ報告すること。

---

## 5. サブエージェント（Subagents）の運用規約

独立して実行可能な具体的・限定的なタスクには、サブエージェント（子エージェント）を積極的に活用する。

- **役割分担**
  - **サブエージェント（調査・定型作業担当）**:
    - コードベースの探索、ファイル・シンボル検索
    - エラーログやログファイルの精査
    - 動作確認・テストの実行検証
    - 機械的・小規模な限定修正
    - ドキュメントやログの下書き・構造化要約
  - **親エージェント（メインAI / 司令塔）**:
    - 全体の設計方針・アーキテクチャの決定
    - ユーザーとの要件定義・曖昧な仕様の整理
    - 広範囲にわたる重要な変更・最終的なコード統合・報告
- **委譲時の必須ルール**
  1. サブエージェントへタスクを依頼する際は「目的」「対象ファイル」「範囲」「期待する成果」「検証基準」を明記する。
  2. 親エージェントとサブエージェントで同一の調査・作業を重複して実行しない。
  3. サブエージェントの作業結果は、必ず親エージェントが確認・精査した上で最終統合・ユーザー報告を行う。

